import { baseUrl } from './site';

export const schemaOrgContext = 'https://schema.org' as const;

export const authorProfileUrl = `${baseUrl}/biography`;

export const authorJsonLdId = `${authorProfileUrl}#person`;
